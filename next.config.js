/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn2.downdetector.com', 'lh3.googleusercontent.com'],
  },
};

module.exports = nextConfig;
